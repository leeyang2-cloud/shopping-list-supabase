gemini mcp add --transport http notion https://mcp.notion.com/mcp
gemini mcp add -s user notion --transport http https://mcp.notion.com/mcp

claude mcp add sequential-thinking -s local -- npx @modelcontextprotocol/server-sequential-thinking@latest
gemini mcp add -s user sequential-thinking -- npx @modelcontextprotocol/server-sequential-thinking@latest


claude mcp add --transport http context7 https://mcp.context7.com/mcp --header "CONTEXT7_API_KEY: YOUR_API_KEY"

setting.json
    "context7": {
      "command": "npx",
      "args": [
		"-y", 
		"@upstash/context7-mcp@latest"
      ]
    }

Use Context7



claude mcp add playwright -- npx @playwright/mcp@latest
gemini mcp add  -s user playwright -- npx @playwright/mcp@latest


claude mcp add --transport http github https://api.githubcopilot.com/mcp -H 'Authorization: Bearer $(grep GITHUB_PAT .env | cut -d '=' -f2)'
claude mcp add --transport http github https://api.githubcopilot.com/mcp -H 'Authorization: Bearer <깃허브 API 토큰>'

setting.json
{
  "mcpServers": {
    "github": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "YOUR_GITHUB_TOKEN_HERE"
      }
    }
  }
}
        "GITHUB_PERSONAL_ACCESS_TOKEN": "여기에_복사한_PAT를_붙여넣기"


claude mcp add --transport http supabase "https://mcp.supabase.com/mcp"
gemini mcp add  -s user supabase  --transport http "https://mcp.supabase.com/mcp"


setting.json
"mcpServers": {
    "supabase": {
      "httpUrl": "https://mcp.supabase.com/mcp"
    }
  }

pw: YOUR_PASSWORD_HERE

YOUR_SUPABASE_TOKEN_HERE


use supabase
/mcp auth supabase